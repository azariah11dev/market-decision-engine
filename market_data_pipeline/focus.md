# market-decision-engine
A production-style market data pipeline that ingests, cleans, validates, features, models, and evaluates financial time-series data, with an explicit focus on failure modes, data leakage, and decision-level evaluation rather than predictive performance.
